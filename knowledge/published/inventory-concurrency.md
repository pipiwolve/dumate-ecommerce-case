# 库存预占并发一致性规范

## 原子边界 {#atomic-boundary}

库存余量校验与预占写入必须处于同一原子边界。仅对最终写入加锁不能防止两个请求
读取同一份余量，因此仍属于 check-then-act 竞态。

## 幂等键 {#idempotency-key}

预占请求必须以订单 ID 作为幂等键。同一订单的重试返回原预占结果，不得重复占用。

## 验收 {#acceptance}

库存为 1 时并发提交两个不同订单，必须有且仅有一个成功；可用库存不得小于零。

